pub fn part_nums(eng_schem: &str) -> u64 {
    let mut sum = 0;
    for line in eng_schem.lines() {
        
    }
    sum
}

#[cfg(test)]
mod test {
    
    #[test]
    fn example() {
        
    }
}