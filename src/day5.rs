pub fn solve(input: &str) -> (u64, u64) {
    let sum_1 = 0;
    let sum_2 = 0;
    
    (sum_1, sum_2)
}

#[cfg(test)]
mod test {
    use super::*;

    #[test]
    fn example() {
        let input =
r#""#;
        assert_eq!((0, 0), solve(input));
    }
}